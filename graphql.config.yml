schema:
  - 'https://graphql.anilist.co'
documents:
  - './shared/**/*.graphql'
generates:
  ./shared/network/src/lib/__generated__/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
